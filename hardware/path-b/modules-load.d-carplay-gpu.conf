# boot-tuning 2026-09-05: load the KMS/GPU drivers in sysinit so the kiosk never races them
# HDMI components are runtime dependencies, not hard vc4 module dependencies.
# Load them first so vc4 does not wait for the general udev coldplug queue.
i2c_brcmstb
snd_soc_hdmi_codec
vc4
v3d
